--- orig/src/private.h
+++ mod/src/private.h
@@ -39,7 +39,7 @@
 	switch (fp->type) {
 	case PF_USE_STDIO:
 		nread = fread(buffer, bytes, 1, fp->fp);
-		if (nread != bytes)
+		if (nread != 1)
 			return NULL;
 		break;
 	case PF_USE_MEMORY:
